spring:
  cloud:
    gcp:
      project-id: ${GCP_PROJECT_ID}
  mail:
    host: ${SMTP_HOST}
    port: ${SMTP_PORT:587}
    username: ${SMTP_USERNAME}
    password: ${SMTP_PASSWORD}

logging:
  level:
    root: INFO
    com.homegenie: INFO
